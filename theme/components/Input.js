import { defineStyleConfig } from "@chakra-ui/react";

export const inputTheme = defineStyleConfig({
  variants: {
    outline: {
      field: {
        background: "#fff",
        borderColor: "#fff",
      },
    },
  },
});
